import { NavLink, Outlet } from 'react-router-dom';
const items = [['/settings/users','Users & Access'],['/settings/positions','Positions & Organogram'],['/settings/permissions','Permission Matrix'],['/settings/modules','System Modules Toggle'],['/settings/document-import','Document Master List Import'],['/settings/evidence','Evidence Upload'],['/settings/actions','Action Tracker'],['/settings/backup','Backup & Restore'],['/settings/devices','Device Access / Pairing']];
export default function SettingsLayout(){return <div><h2>Settings</h2><p>Administrative foundation for host setup, access control, modules, backups, and LAN client preparation.</p><div className="settings-layout"><nav className="settings-nav">{items.map(([to,label])=><NavLink key={to} to={to}>{label}</NavLink>)}</nav><div><Outlet/></div></div></div>}
